import { describe, it, expect } from "vitest";

// Phase 0 placeholder. Real unit suites land per-phase (format, scoring, api).
describe("phase 0 bootstrap", () => {
  it("test runner is wired", () => {
    expect(true).toBe(true);
  });
});
