import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function ResearchArchive() {
  return (
    <PagePlaceholder
      title="Research Briefings Archive"
      phase="Phase 5"
      spec="HANDOFF §4.5 — BriefingList, BriefingReader, HandoverViewer, ThemeDrift"
    />
  );
}
