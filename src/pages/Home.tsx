import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function Home() {
  return (
    <PagePlaceholder
      title="Today's Opportunities & Signal Decisions"
      phase="Phase 2"
      spec="HANDOFF §4.1 — StatusStrip, ThemeCard, FilterBar, OpportunityTable, ReconcileChip"
    />
  );
}
