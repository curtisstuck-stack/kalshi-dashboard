import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function Portfolio() {
  return (
    <PagePlaceholder
      title="Paper + Live Portfolio"
      phase="Phase 3"
      spec="HANDOFF §4.3 — KPICards, EquityCurve, OpenPositionsTable, FillsTable, SettlementTable, AttributionChart"
    />
  );
}
